import styles from './UncontrolledForm.module.sass';

const UncontrolledForm: React.FC = (): JSX.Element => (
  <>
    <h1 className={styles.heading}>Uncontrolled Form</h1>
  </>
);

export default UncontrolledForm;
